# 前端环境变量示例
# 使用方式：复制为 .env.development 或 .env.production 后按需修改
# 注意：前端默认已由 Vite 代理转发到后端（见 vite.config.ts），通常无需修改

# 接口基础路径
VITE_API_BASE_URL=/api

# 后端直连地址（仅在需要绕过 Vite 代理时使用）
VITE_API_ORIGIN=http://localhost:8080
